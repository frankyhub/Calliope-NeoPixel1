let neo = neopixel.create(DigitalPin.P1, 1, NeoPixelMode.RGB_RGB)
neo.setBrightness(20)
basic.forever(function () {
    for (let index = 0; index < 1; index++) {
        neo.showRainbow(1, 3)
        neo.show()
        basic.pause(5000)
        basic.pause(5000)
    }
})
